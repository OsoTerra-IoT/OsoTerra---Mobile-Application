<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema base. La apariencia real la define Compose (ui/theme). Este tema solo
         evita la ActionBar del sistema y fija el color de fondo de la ventana. -->
    <style name="Theme.OsoTerra" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor">@color/osoterra_green</item>
    </style>
</resources>
